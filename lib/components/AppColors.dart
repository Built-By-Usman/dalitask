import 'package:flutter/material.dart';

class AppColors {
  // Basic Colors
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const blackLight = Color(0xFF4A4A4A);   // Cool dark grey for subheadings
  static const grey = Color(0xFF9E9E9E);          // Medium grey for secondary text
  static const greyLight = Color(0xFFF5F5F5);     // Light grey for backgrounds

  // Green Shades (Cooler greens)
  static const green = Color(0xFF0B8F4D);         // Primary Green
  static const greenLight = Color(0xFF3FC68F);    // Light Green
  static const greenLightest = Color(0xFFD7FDEA); // Very light green

  // Orange Shades (Soft warm tones)
  static const orange = Color(0xFFFFA726);        // Primary Orange
  static const orangeLight = Color(0xFFFFCC80);   // Light Orange

  // Gold Shades (Muted gold)
  static const gold = Color(0xFFE6C85B);          // Gold
  static const goldLight = Color(0xFFF5EEDC);     // Light Gold

  // Foregrounds for text/icons on colored backgrounds
  static const greenForeground = Color(0xFFE6F5EC);   // Soft green background
  static const orangeForeground = Color(0xFFFFF2E6);  // Soft orange background

  // Red Shades
  static const red = Color(0xFFD32F2F);          // Primary Red
  static const redLight = Color(0xFFFFCDD2);     // Light Red
}